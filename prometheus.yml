global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/suricata.rules

scrape_configs:
  - job_name: 'suricata'
    static_configs:
      - targets: ['suricata-exporter:9100']
    scrape_interval: 10s
    metrics_path: /metrics
